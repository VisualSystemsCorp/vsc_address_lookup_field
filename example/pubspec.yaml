name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  vsc_address_lookup_field:
    path: ..
  flutter_dotenv: ^5.0.2
  url_launcher: ^6.1.11
  google_place:
    git:
      url: https://github.com/dsyrstad/google_place.git
      ref: HEAD

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
